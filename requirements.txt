# AI Agent Guards Testing Application Requirements
# Optimized for Streamlit Cloud deployment with numpy 2.x compatibility

# NumPy first to avoid binary compatibility issues
numpy>=2.1.1,<3.0.0

# UI Requirements for Streamlit demo (install first to avoid conflicts)
streamlit>=1.37.0,<2.0.0
plotly>=5.17.0,<6.0.0
pandas>=2.0.0,<3.0.0

# Core utilities
python-dotenv>=1.0.0,<2.0.0
requests>=2.28.0,<3.0.0

# Core LlamaFirewall for scanner functionality (with constraints)
llamafirewall>=1.0.3,<2.0.0

# NeMo GuardRails for fact-checking, self-contradiction, and hallucination detection (with constraints)
nemoguardrails>=0.16.0,<1.0.0

# spaCy model required by NeMo GuardRails
# NOTE: Commented out for Streamlit Cloud Python 3.13 compatibility
# The app will download this at runtime if needed, or fall back to pattern-only mode
# https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Microsoft Presidio for PII detection in DataDisclosureGuard (2.2.356+ has numpy 2.x support)
presidio-analyzer>=2.2.356,<3.0.0
presidio-anonymizer>=2.2.356,<3.0.0

# Note: spaCy models are optional for Presidio and NeMo
# Pattern-based recognizers (SSN, credit cards, fact-checking) work without spaCy
# This makes the app compatible with Streamlit Cloud's restricted environment

# Pinned transitive dependencies to prevent pip backtracking (saves 10+ min in Docker builds)
click>=8.0.0,<9.0.0
protobuf>=5.29.5,<6
pydantic>=2.0.0,<3.0.0
onnxruntime==1.20.1
onnx==1.17.0
semgrep==1.121.0
fastembed==0.5.1
transformers>=4.51.3,<5.0.0
pillow>=10.4.0,<13
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-proto==1.25.0
opentelemetry-exporter-otlp-proto-common==1.25.0
opentelemetry-exporter-otlp-proto-http==1.25.0
