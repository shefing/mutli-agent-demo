# Minimal requirements for AI Agent Guards Testing Application
# Install these first to avoid dependency conflicts

# Core UI components
streamlit>=1.28.0
plotly>=5.17.0
pandas>=2.0.0
python-dotenv>=1.0.0

# Core libraries without heavy dependencies
pydantic>=2.0.0
requests>=2.28.0  # Required for AlignmentCheck API calls

# OpenAI SDK version constraint (compatibility with NeMo GuardRails)
# Issue: OpenAI SDK 1.58.0+ has breaking changes that cause stream_usage parameter errors
# with langchain-community (used by NeMo GuardRails). Pin to earlier version until fixed.
# See NEMO_GUARDRAILS_FIX.md for details.
openai<1.58.0

# httpx version constraint (compatibility with LlamaFirewall + OpenAI SDK)
# Issue: httpx 0.28.0 removed the `proxies` parameter, which breaks
# LlamaFirewall and OpenAI SDK's httpx.Client() calls.
httpx<0.28.0